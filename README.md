# Hello Java Project

This is a simple Java project initialized with Gradle. It contains a basic
`App` class that prints "Hello, world!" and a placeholder test using JUnit.

## Build and Run

```
# Compile and run tests
gradle build

# Run the application
gradle run
```
